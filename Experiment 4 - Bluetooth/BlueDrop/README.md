# BlueDrop

BlueDrop is a Bluetooth Low Energy (BLE) file transfer application built with React Native (Expo).

## Features
- Scan for nearby devices
- Connect and transfer files (images/text)
- Offline capability

## Setup
1. `npm install`
2. `npx expo run:android`
